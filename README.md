# InfoGen

A Python-based tool to generate data-driven infographics.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Edit `data.json` to update stats.

3. Run generation:
   ```bash
   python main.py
   ```

## Output
Generates `output_infographic.png`.
